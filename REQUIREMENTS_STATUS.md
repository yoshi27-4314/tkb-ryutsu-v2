# テイクバック流通 v2 - 要件充足レポート

作成日: 2026-04-22
対照元: `firsteight-app/report_all_requirements.html` (2026-04-21作成)

---

## よしさんの全要望リスト（#1〜#33）対照結果

### サマリー

| 状態 | 件数 |
|------|------|
| 実装済み | 14 |
| 部分実装 | 7 |
| 未実装 | 12 |

---

### #1 分荷判定（写真→AI→チャンネル決定→管理番号発行）
**状態: 実装済み**

- `intake/index.js` の `handleAIJudgment()` (L280-330) で Edge Function `takeback-judge` にPOSTしてAI判定を実行
- `handleConfirm()` (L488-559) で `db.generateMgmtNum()` により管理番号を採番（YYMM-連番4桁、RPC `next_mgmt_num` 使用、フォールバック付き）
- 9種類のチャンネルが `config.js` L14-26 で定義済み
- `checkNeedsApproval()` (L472-484) で浅野承認ルール（信頼度<0.7、価格30000円以上、高スコア）を実装
- 状態ランク・スコア・想定価格・開始価格・目標価格を `renderResult()` (L334-451) で表示
- OK/相談/再判定ボタン実装済み

### #2 バーコード登録（ISBN→書籍判定）
**状態: 実装済み**

- `intake/index.js` L192-205 にバーコード入力フィールド（仕入先が自社仕入の場合のみ表示）
- `handleBarcodeScan()` (L254-276) で `BarcodeDetector` APIによる読み取り
- AI判定時に `step: 'book'` と `bookInfo: { isbn }` を送信 (L291-296)

### #3 在庫一覧・検索・フィルター
**状態: 実装済み**

- `sales/index.js` にタブ切り替え (出品待ち/出品中/全件) と検索バー (L63-108)
- `trade/index.js` にも検索バーとタブ (落札済み/梱包待ち/発送準備/全件) (L136-224)
- `db.js` の `getItems()` (L47-78) でステータスフィルタ、キーワード検索（商品名/管理番号/メーカー/出品タイトル）、ソート、ページネーション対応
- ホーム画面のステータスカードからモジュールへの遷移も可能 (app.js L204-219)

### #4 出荷登録（送り状OCR対応）
**状態: 実装済み**

- `trade/index.js` の `renderShippingScreen()` (L918-1088) で出荷フロー実装
- 送り状OCR: L998-1038 で `capturePhoto()` → Edge Function (`type: 'tracking'`) → 追跡番号自動入力
- 運送会社選択（7社: 佐川/アート/西濃/ヤマト/郵便/直接引き取り/後日発送）
- E飛伝CSV出力: `handleEhidenCsvExport()` (L1094-1109) + `generateEhidenCsv()` (L1111-1169)

### #5 経費精算（レシートOCR）
**状態: 部分実装**

- `ops/index.js` の `renderExpenseRegister()` (L191-322) でレシートOCR + 手入力登録
- OCR結果から日付/店名/金額/税率/科目/インボイス番号を自動入力 (L324-360)
- 勘定科目13種類、支払方法3種（現金/クレジット/立替）の選択肢あり (L22-28)
- インボイス番号入力＋バリデーション (T+13桁) 実装済み (L229, L288-289)
- 税計算実装済み: `taxAmount = Math.round(amount * taxRate / (100 + taxRate))` (L293)
- 月別一覧 `renderExpenseList()` (L362-409) と月別集計 `renderExpenseSummary()` (L411-463) 実装済み
- **未実装**: 立替精算フロー（支払方法に「立替」選択肢はあるが、立替者の記録・精算フローがない）
- **未実装**: 小口現金の残高管理

### #6 売上登録（スクショOCR + 手入力、粗利自動計算）
**状態: 実装済み**

- `trade/index.js` の `handleTransactionOcr()` (L451-484) でスクショOCR
- `renderSalesEditForm()` (L634-769) で手入力フォーム（落札価格/手数料/送料/梱包費/仕入原価）
- 粗利自動計算 (L698-714): `gp = sp - pf - sc - pc - ac`
- `db.createSale()` で売上レコード作成

### #7 Google Chat連携 + AI相談
**状態: 実装済み**

- `ops/index.js` の `renderChatRooms()` (L993-1009) で5つのGoogle Chatルームへのリンク
- `renderAIChat()` (L1011-1117) でAI相談機能（テンプレート6種＋自由入力、会話履歴保持）

### #8 出退勤（アナログ時計ピッカー + 月間カレンダー）
**状態: 実装済み**

- `ops/index.js` の `renderClock()` (L496-687) で打刻画面
- `openClockPicker()` (L690-785) で24時間対応アナログ時計ピッカー（時→分の2ステップUI）
- `renderAttendanceCalendar()` (L788-887) で月間カレンダー（出勤日数/総労働時間/平均表示）
- 実労働時間自動計算: 出勤-退勤-休憩 (L573-589)

### #9 休み・遅刻・早退の連絡
**状態: 実装済み**

- `ops/index.js` の `renderLeaveNotice()` (L890-963) で届出フォーム
- 種類: 欠勤/遅刻/早退/有給休暇/その他
- `db.createLeaveNotice()` でDB保存

### #10 当番ローテーション表示
**状態: 実装済み**

- `config.js` L166-172 に曜日別ローテーション定義（月〜金、分荷撮影/出品/取引ナビ/梱包出荷）
- `app.js` のホーム画面 (L239-248) と `ops/index.js` の KPI画面 (L1189) で表示
- トイレ掃除当番については `CLEANING_EXCLUDED` (L174) が定義されているが、表示UIは未確認

### #11 チームKPI進捗バー
**状態: 実装済み**

- `app.js` のホーム画面 L178-199 で分荷/出品/梱包/出荷の実績+進捗バー表示
- `ops/index.js` の `renderKPI()` (L1139-1198) + `kpiCard()` (L1200-1218) で詳細KPI表示
- `config.js` L135-139 に日次目標（分荷58/出品53/梱包16）

### #12 個人実績（マイページ、本人のみ表示）
**状態: 実装済み**

- `ops/index.js` の `renderMyPage()` (L1317-1461) でプロフィール・アバター・機能ガイド・更新履歴表示
- スタッフタイムライン `renderStaffTimeline()` (L1271-1312) で全スタッフの出退勤表示
- **注意**: 個人の分荷/出品/梱包実績の詳細グラフはまだない（KPIはチーム全体のみ）

### #13 出品画面の実装（在庫タブ→出品待ち→出品作業）
**状態: 部分実装**

- `sales/index.js` の `openListingWork()` (L206-474) で出品作業画面
  - ロック機構（排他制御）: `db.lockItem()` / `db.unlockItem()` / `db.cleanStaleLocks()`
  - タイマー: `startTimer()` / `stopTimer()` (L863-895)
  - AI生成タイトル・説明文: `generateAIListing()` (L479-564)
  - タイトルバリデーション: 65文字制限 + 禁止文字チェック (L33-34, L415-428)
  - スタッフマーク自動付与: `CONFIG.STAFF_MARKS` を参照してタイトル先頭に付与 (L515-521)
  - 価格設定・チャンネル変更・コピーボタン
  - 途中保存 `saveProgress()` (L674-691)
- 出品中の詳細画面: `openListingDetail()` (L696-858) で価格変更・チャンネル変更・再出品
- **改善点**: 写真ギャラリーは表示のみ（並べ替え・削除・差替は未実装）

### #14 スプレッドシートからの在庫データ読み込み
**状態: 未実装**

- v2コードにスプレッドシート読み込みの実装なし
- GAS連携のコードも存在しない

### #15 Monday.com解約準備（全データ転記完了確認）
**状態: 部分実装**

- v2はSupabase DBベースで動作しており、Monday.com依存を排除済み
- `config.js` にMonday.com関連の設定なし
- **未完了**: 152ボードからの全データ移行確認ツールは未実装

### #16 カレンダー（Google Calendar + TimeTree風UI）
**状態: 未実装**

- ボトムナビにカレンダータブなし
- 勤怠カレンダーはあるが、Google Calendar連携やチーム予定管理は未実装

### #17 共有ドライブ権限移譲（三島退職4/30）
**状態: 未実装**

- アプリの範囲外（手動対応が必要）
- v2コードに関連実装なし

### #18 freeeデータ→アプリのスタッフ一覧に反映
**状態: 未実装**

- `config.js` の `STAFF_MARKS` がハードコードされたスタッフ一覧
- freee APIからの従業員マスタ取得は未実装

### #19 アプリの出退勤→freee日次勤怠APIに連動
**状態: 部分実装**

- 勤怠画面に `synced_to_freee` フラグの表示あり (ops/index.js L636-644, L864)
- DB `attendance` テーブルに `synced_to_freee` カラムを想定
- **未実装**: 実際のfreee APIへの送信処理は存在しない

### #20 小口現金管理（残高表示 + レシートOCR連動）
**状態: 未実装**

- 経費精算で「現金」支払方法は選択可能だが、小口現金の残高管理UIなし

### #21 雇用契約書テンプレート
**状態: 未実装**

- v2コードに関連実装なし

### #22 クリアメンテのfreee連携
**状態: 未実装**

- 部門に「クリアメンテ」の選択肢あり (ops/index.js L30) だが、freee連携は未実装

### #23 新しい声ポイント制度の移行
**状態: 未実装**

- v2コードにポイント制度関連の実装なし

### #24 経費精算の立替・月別集計・インボイス対応
**状態: 部分実装**

- インボイス番号入力＋バリデーション: 実装済み (ops/index.js L229, L288-289)
- OCRからのインボイス番号自動抽出: 実装済み (ops/index.js L352-355)
- 月別集計: `renderExpenseSummary()` (L411-463) で科目別集計表示 実装済み
- 月別一覧: `renderExpenseList()` (L362-409) で月選択+合計表示 実装済み
- 税計算: 実装済み (L293)
- **未実装**: 立替者の記録フロー（「誰が立て替えたか」の記録と精算フロー）
- **未実装**: 支払方法別タブ表示

### #25 管理番号の欠落・不一致の修復
**状態: 未実装**

- データ修復ツールは未実装
- 管理番号の採番はRPCベース (`db.generateMgmtNum()`) で改善されているが、既存データの修復UIなし

### #26 出品説明文の一括AI生成（全164件）
**状態: 未実装**

- 個別のAI生成は `sales/index.js` `generateAIListing()` (L479-564) で可能
- 一括生成のバッチ処理は未実装

### #27 特別ステータスの実装（配送トラブル→返送→確認→キャンセル/返金）
**状態: 部分実装**

- `config.js` L53-62 にトラブル系ステータス10種を定義
- `trade/index.js` の `renderTroubleScreen()` (L1192-1267) でトラブル報告UI
  - トラブル種別選択: 商品問題連絡/運送会社相談中/キャンセル処理/返金処理
  - 詳細メモ入力
- `handleTroubleNext()` (L1280-1315) でトラブルフローのステップ進行
- `TROUBLE_FLOW` (L54-57) で7段階のトラブルフロー定義
- **課題**: 運送会社請求・入金確認のフローは定義のみでUIが簡素

### #28 取引先受取日管理（渡辺質店等）
**状態: 未実装**

- `intake/index.js` L12-17 に仕入先として「渡辺質店」「ビッグスポーツ」等の定義あり
- 受取日の記録や空白期間の可視化は未実装

### #29 担当マーク自動付与（〇/▽/☆等）
**状態: 実装済み**

- `config.js` L156-163 に全6名のスタッフマーク定義
- `sales/index.js` L245-246 で `CONFIG.STAFF_MARKS[staff.name]` を取得
- AI生成タイトルの先頭にマーク自動付与 (L515-521): `mark ? \`${mark}${data.title}\` : data.title`
- 出品作業画面にマーク表示 (L308-310)

### #30 画像の並べ替え・差替・削除
**状態: 未実装**

- `sales/index.js` L282-291 で写真ギャラリーは表示のみ
- ドラッグ＆ドロップ並替、差替、削除のUIなし

### #31 知識インプット・蓄積機能
**状態: 未実装**

- v2コードに関連実装なし

### #32 リードタイム自動計算・表示
**状態: 未実装**

- 各工程の日時は記録されている（`judged_at`, `listed_at`, `packed_at`, `shipped_at`, `completed_at`）
- 工程間のリードタイム計算・可視化UIは未実装

### #33 出荷完了→Google Chat/チーム通知
**状態: 未実装**

- 出荷完了処理 (trade/index.js L1041-1087) にチーム通知なし

---

## Slack Bot 73機能の v2 対照

### 1. 分荷判定チャンネル（北大路魯山人）— 15機能

| # | 機能 | レポート状態 | v2状態 | 根拠 |
|---|------|------------|--------|------|
| 1 | 商品写真からAI自動判定 | 実装済み | 実装済み | intake/index.js `handleAIJudgment()` L280-330 |
| 2 | 9種類の販売チャンネル自動決定 | 実装済み | 実装済み | config.js L14-26 に11チャンネル定義 |
| 3 | 管理番号自動発行 (YYMM-連番4桁) | GAS依存 | 改善済み | db.js `generateMgmtNum()` L148-167、SupabaseのRPC `next_mgmt_num` 使用 |
| 4 | スコア計算（100点満点） | 実装済み | 実装済み | AI判定結果の `score` をDB保存、表示 (intake/index.js L379) |
| 5 | 浅野承認ルール（7条件） | 部分 | 部分実装 | `checkNeedsApproval()` L472-484 で3条件（信頼度/高額/高スコア）チェック。希少品/骨董/貴金属/含み益の判定はAI任せ |
| 6 | コマンド: 確定/相談/再判定/キャンセル | 実装済み | 実装済み | L404-446 にOK/相談/再判定/撮り直しボタン |
| 7 | 在庫検索コマンド | 実装済み | 実装済み | sales/index.js 検索バー + db.js `getItems()` |
| 8 | Monday.com自動登録 | 実装済み | 実装済み | Supabase DB (`db.createItem()`) に移行済み |
| 9 | スプレッドシート自動転記 | GAS応答不可 | 未実装 | v2にGAS連携なし。Supabase直接保存に一本化 |
| 10 | 高額品→浅野自動メンション | 未実装 | 未実装 | 通知機能なし。`checkNeedsApproval()` で警告表示のみ |
| 11 | まとめ売り判定 | 実装済み | 部分実装 | config.js にチャンネル `accepts_matome: true` あるが、まとめ売り用UIは専用なし |
| 12 | 予想販売価格（範囲） | 実装済み | 実装済み | intake/index.js L380 `estimatedPriceMin〜estimatedPriceMax` |
| 13 | 推奨スタート価格/目標価格 | 実装済み | 実装済み | intake/index.js L381-382 `startPrice` / `targetPrice` |
| 14 | 予測在庫期間/在庫期限 | 未実装 | 未実装 | v2にも該当フィールドなし |
| 15 | 市場需要レベル(1〜3) | 未実装 | 未実装 | v2にも該当フィールドなし |

### 2. 撮影・採寸・保管チャンネル（白洲次郎）— 8機能

| # | 機能 | レポート状態 | v2状態 | 根拠 |
|---|------|------------|--------|------|
| 16 | テプラ画像から管理番号OCR | 未実装 | 未実装 | v2にも該当実装なし |
| 17 | 撮影ガイダンス（3〜5枚推奨） | 実装済み | 実装済み | intake/index.js L34-41 `PHOTO_SLOTS` 6枠定義（正面/背面/状態/タグ/追加x2）|
| 18 | 写真→Google Drive自動保存 | 実装済み | 実装済み | intake/index.js `uploadToDrive()` L1047-1069 |
| 19 | 採寸入力（3次元サイズ） | 実装済み | 実装済み | intake/index.js `renderPhotoStep()` L700-707 (幅/高さ/奥行/重さ) |
| 20 | ロケーション入力（棚番） | 実装済み | 実装済み | intake/index.js `renderStorageStep()` L822-926 (拠点→エリアの階層選択) |
| 21 | 修正コマンド（メニュー選択式） | 部分 | 部分実装 | 完了画面から「写真を追加」「保管場所を変更」は可能 (L1006-1015)。サイズ修正は未対応 |
| 22 | Monday.comメイン写真アップロード | 不要 | 不要 | Monday.com廃止のため |
| 23 | リードタイム自動計算 | 未実装 | 未実装 | 各工程の日時は記録されるが、リードタイム表示なし |

### 3. 出品・保管チャンネル（岩崎弥太郎）— 8機能

| # | 機能 | レポート状態 | v2状態 | 根拠 |
|---|------|------------|--------|------|
| 24 | 出品タイトル自動生成 | 実装済み | 実装済み | sales/index.js `generateAIListing()` L479-564 |
| 25 | 説明文自動生成 | 実装済み | 実装済み | 同上、`type === 'description'` で分岐 |
| 26 | 開始価格提案 | 実装済み | 実装済み | AI判定結果 `startPrice` + 出品画面の価格設定 |
| 27 | 担当マーク自動付与 | 未実装→実装済み | 実装済み | sales/index.js L515-521 でタイトル先頭にマーク付与 |
| 28 | 画像管理コマンド | 未実装 | 未実装 | 並べ替え/削除/差替なし |
| 29 | ページ作成完了→時刻記録 | 実装済み | 実装済み | sales/index.js タイマー L863-895、`listing_duration_seconds` 保存 |
| 30 | 出品確定→棚番入力 | 実装済み | 実装済み | intake/index.js のストレージステップで入力済み |
| 31 | 出品タイトルルール（65文字/禁止文字） | 部分 | 実装済み | sales/index.js L33-34 `TITLE_MAX_LEN=65`, `FORBIDDEN_CHARS`、L415-428 でバリデーション |

### 4. 梱包・出荷チャンネル（黒田官兵衛）— 6機能

| # | 機能 | レポート状態 | v2状態 | 根拠 |
|---|------|------------|--------|------|
| 32 | 管理番号→商品情報取得・表示 | 実装済み | 実装済み | trade/index.js `renderDetail()` L268-439 |
| 33 | 運送会社選択（5社） | 部分（4社） | 改善（7社） | config.js L108-116: 佐川/アート/西濃/ヤマト/郵便/直接引取/後日発送 |
| 34 | 送り状OCR（Claude Haiku Vision） | 実装済み | 実装済み | trade/index.js L998-1038 |
| 35 | 後日発送対応 | 部分 | 部分実装 | 運送会社に「後日発送」選択肢あり (config.js L115) だが、後日伝票番号入力フローは未実装 |
| 36 | 通販コミュニティ通知 | 未実装 | 未実装 | 出荷完了時の自動通知なし |
| 37 | E飛伝CSV出力 | 実装済み | 実装済み | trade/index.js `handleEhidenCsvExport()` L1094-1186 |

### 5. 現場査定チャンネル（渋沢栄一）— 5機能

| # | 機能 | レポート状態 | v2状態 | 根拠 |
|---|------|------------|--------|------|
| 38-42 | 販売価格査定/廃棄判断/知識蓄積/古物台帳/スクラップ計算機 | テイクバック再生の領域 | 対象外 | 知識蓄積のみ流通アプリの要望 (#31) だが未実装 |

### 6. ステータス確認チャンネル（ステータス松本）— 5機能

| # | 機能 | レポート状態 | v2状態 | 根拠 |
|---|------|------------|--------|------|
| 43 | 全体ステータスサマリー | 実装済み | 実装済み | app.js L202-219 + ops/index.js `renderStatusDistribution()` L1220-1238。`STATUS_FLOW` 17種 |
| 44 | ボトルネック警告 | 実装済み | 実装済み | app.js L153-156、ops/index.js L1152-1159 (出品待ち>100, 梱包待ち>10 等) |
| 45 | 浅野専用ダッシュボード | 実装済み（別アプリ） | 対象外 | executive.html は別アプリ（firsteight-executive） |
| 46 | 実績レポート（期間選択） | 実装済み（別アプリ） | 対象外 | executive.html の経営タブ |
| 47 | 個別管理番号のステータス照会 | 実装済み | 実装済み | 検索バーで管理番号入力 → 詳細表示 |

### 7. 出退勤チャンネル（二宮金次郎）— 5機能

| # | 機能 | レポート状態 | v2状態 | 根拠 |
|---|------|------------|--------|------|
| 48 | 勤務時間申告 | 実装済み | 実装済み | ops/index.js `renderClock()` L496-687 |
| 49 | 自動計算（総時間-休憩=実働） | 実装済み | 実装済み | `calcWork()` L573-589 |
| 50 | 代筆モード | 部分 | 実装済み | ops/index.js L510-519 管理者限定の代筆トグル+スタッフ選択 |
| 51 | 本日の作業実績自動集計 | 部分 | 部分実装 | KPI画面に今日の実績表示あり。勤怠記録時の自動集計連動はなし |
| 52 | freee人事労務API連動 | 部分 | 部分実装 | `synced_to_freee` フラグ表示のみ。実際のAPI送信なし |

### 8. 新しい声ポイント制度 — 4機能

| # | 機能 | レポート状態 | v2状態 | 根拠 |
|---|------|------------|--------|------|
| 53-56 | 要望投稿/3層通知/ポイントシステム/四半期支払い | 全て未実装 | 全て未実装 | v2にも該当実装なし |

### 9. 経費精算（福沢諭吉）— 7機能

| # | 機能 | レポート状態 | v2状態 | 根拠 |
|---|------|------------|--------|------|
| 57 | レシートOCR（Claude Vision） | 実装済み | 実装済み | ops/index.js L241-268 |
| 58 | 勘定科目自動判定 | 実装済み | 実装済み | OCR結果から `result.category` を自動設定 (L346-351) |
| 59 | インボイス番号自動抽出 | 未実装→実装済み | 実装済み | OCR結果 `result.invoiceNumber` を自動設定 (L352-355) + 手入力 + バリデーション |
| 60 | 立替経費対応 | 未実装 | 未実装 | 支払方法に「立替」はあるが、立替者記録・精算フローなし |
| 61 | 支払方法別タブ | 未実装 | 未実装 | 一覧は支払方法を表示するが、タブでの絞り込みなし |
| 62 | 税計算自動化 | 未実装→実装済み | 実装済み | L293 `taxAmount = Math.round(amount * taxRate / (100 + taxRate))` |
| 63 | 月別集計 | 未実装→実装済み | 実装済み | `renderExpenseSummary()` L411-463 で科目別集計+月選択 |

---

## レポートからの状態変化まとめ（v2で改善された項目）

以下はレポート時点で未実装/部分実装だったが、v2コードで改善されている項目:

1. **管理番号自動発行**: GAS依存 → Supabase RPC (`next_mgmt_num`) に移行。フォールバック付き
2. **運送会社選択**: 4社 → 7社（アートデリバリー追加、直接引取/後日発送追加）
3. **担当マーク自動付与**: 未実装 → `sales/index.js` で実装済み
4. **出品タイトルルール**: バリデーションなし → 65文字制限+禁止文字チェック実装
5. **インボイス番号**: 未実装 → 入力+バリデーション+OCR自動抽出
6. **税計算自動化**: 未実装 → 税込金額から逆算
7. **月別集計**: 未実装 → 科目別集計+バーチャート表示
8. **代筆モード**: 不明 → 管理者限定で実装済み

---

## 技術的問題の改善状況

| # | レポートの問題 | v2状態 |
|---|-------------|--------|
| 1 | GAS応答が読めない (mode:'no-cors') | 解消: GAS連携を廃止し、Supabase直接保存に統一 |
| 2 | ローカルストレージとDB混在 | 解消: 全てSupabase DB (`db.js`) に統一。localStorageはテーマ/アバターのみ |
| 3 | Edge Function依存のリスク | 継続: AI判定・Drive保存は引き続きEdge Function経由。接続切れの監視なし |
| 4 | APIタイムアウト未設定 | 継続: fetch()にタイムアウト設定なし |
| 5 | ローカル採番のフォールバック | 改善: RPC `next_mgmt_num` でアトミック採番。フォールバックはテーブル参照 |
| 6 | XSSリスク | 継続: `escapeHtml()` は使用されているが、innerHTML多用は継続 |
| 7 | Supabase Anon Key公開 | 継続: config.js L11 にキー直書き |
| 8 | localStorage容量超過 | 改善: localStorage使用を最小限に限定（テーマ/アバター/背景のみ） |

---

## 優先的に取り組むべき未実装項目

### 最優先（業務直結）
1. **#33 出荷完了→チーム通知** — Google Chat Webhook等で実装可能
2. **#32 リードタイム計算・表示** — 日時データは全て記録済み。表示UIのみ必要
3. **#27 トラブルフロー充実** — 基本実装済み。運送会社請求/入金確認のUI追加

### 高優先（業務効率）
4. **#30 画像の並べ替え・差替・削除** — 出品作業の効率向上
5. **#24 立替経費フロー** — 支払方法の選択肢はあるが、精算フローがない
6. **#19 freee勤怠API連動** — フラグ表示のみ。実API送信の実装

### 中優先（運用改善）
7. **#26 出品説明文の一括AI生成** — バッチ処理の実装
8. **#16 カレンダー機能** — Google Calendar連携
9. **#23 新しい声ポイント制度** — 新規テーブル+UIの構築
10. **#31 知識蓄積機能** — メモDB+検索UI
